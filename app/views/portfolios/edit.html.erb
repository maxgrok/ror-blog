<h1 style="text-align: center;">Edit Portfolio Item</h1>

<!-- form will be here -->
<div class="container">
<%= form_with(model: @portfolio_item, local: true) do |form| %>
  <% if @portfolio_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(blog.errors.count, "error") %> prohibited this blog from being saved:</h2>

      <ul>
      <% @portfolio_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, placeholder: "#{@portfolio_item.title}" %>
  </div>

  <div class="field">
    <%= form.label :subtitle %>
    <%= form.text_field :subtitle %>
  </div>
	
	<div class="field">
    <%= form.label :body %>
    <%= form.text_area :body, :class=>"materialize-textarea"  %>
  </div>
	
	<div class="field">
    <%= form.label :thumb_image %>
    <%= form.text_field :thumb_image %>
  </div>
		<div class="field">
    <%= form.label :main_image %>
    <%= form.text_field :main_image %>
  </div>

  <div class="actions">
    <%= form.submit "Edit Portfolio Item", :class => 
    'waves-effect waves-light btn', :style =>'background-color: #63C174; font-color: white;' %>
  </div>
<% end %>
</div>